import { Text, View } from "@tarojs/components";
import React from "react";
import styles from "./index.module.less";
import cs from "classnames";


const Index = (props) => {
  const { } = props;
  return (
    <View className={styles.dayStatic}>
    </View>
  );
};

export default Index;
